

Different kinds of objects can share different characteristics, however, an iMac can run OSX while an android runs on android or dell
runs on windows.

While objects can share characteristics - they can also be different as well

We want to create classes that can inherit features of other classes